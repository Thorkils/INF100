/**
* Fag: INF100
* Øving: 3
* Oppgave: 1
* Dato: 02.09.2015
*
* Konverterer fra/til NOK/EUR
* @author Thorkil Schjodt
*/

import java.util.Scanner;

public class HultRektangel {
  public static void main ( String [] args ) {
       
    //setter tastatur som scanner
    Scanner tastatur = new Scanner(System.in);    
    
    
    System.out.println("Hoyde:");
    
    int hoyde = tastatur.nextInt();
    
    if (hoyde < 1){ //Hvis bruker oppgir en ugyldig verdi
      System.out.println("Ugyldig verdi");
    }
    
    else {
      System.out.println("Bredde:");
    
    
    int bredde = tastatur.nextInt();
      
    if (bredde < 1){ //Hvis bruker oppgir en ugyldig verdi
      System.out.println("Ugyldig verdi");
    }
    
    else {
   
      
     ////////////////////////////////////////////////////
    ////////////////////REKTANGELKODE/////////////////// 
   ////////////////////////////////////////////////////  
      
      
      for(int linjeNr=0; linjeNr<hoyde; linjeNr++) {
       
        
        if(bredde == 1){ //Hvis bredde er 1
          if (linjeNr == 0 || linjeNr == (hoyde-1)){
            System.out.println("*");
          }
          else {
          System.out.println("|");
          }
          
        }
          
          else if (linjeNr == 0 || linjeNr == (hoyde-1)){ //For forste og siste linje
            
            //System.out.println("Sjekker om linjeNr == 0 || linjeNr == (hoyde-1)");
            System.out.print("*");
            
            for(int breddeNr = 0; breddeNr<(bredde - 2); breddeNr++){
              System.out.print("-");  
            }
           
            System.out.println("*");
            
          } 
          else if (linjeNr > 0 && linjeNr < (hoyde-1)){ //For alle andre enn forste og siste linje 
            
            //System.out.println("Sjekker om linjeNr > 0 || linjeNr < (hoyde-1)");
            System.out.print("|");
            
            for(int breddeNr =0; breddeNr<(bredde - 2); breddeNr++){
              System.out.print(" ");  
            }
           
            System.out.println("|");
            
          }
      }

    }
     
    }
    
  }
}
